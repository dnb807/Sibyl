[project]
name = "fortuneteller"
version = "1.0.0"
description = "Determine correlation between polymarket outcomes with tweets from policy makers"
dependencies = [
    "flask (==3.1.1)",
    "psycopg2 (==2.9.10)",
]

[tool.setuptools]
py-modules = []